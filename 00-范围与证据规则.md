# StarAsk 网站版产品架构逆向拆解｜范围与证据规则

- 产品：StarAsk 星问（官网 `www.starask.io` 与网站版应用 `app.starask.io`）
- 快照时间：2026-08-19（Asia/Shanghai）
- 访问级别：公开官网 + 网站版自动建立的访客模式；未绑定邮箱、Google 或 Apple
- 分析方法：`product-architecture-teardown v2`，辅以 evidence-led product teardown 证据协议
- 允许动作：只读导航、展开菜单/弹窗、切换展示周期、查看访客可见状态
- 未执行动作：未提交出生信息、问题、照片或问卷；未发送语音；未邀请好友；未订阅、支付、删除或开启通知
- 停止点：任何需要传输个人/敏感资料、消耗额度、生成报告、建立对话、购买或对外分享的动作之前

## 证据等级

- `【已确认】`：本次可见 UI、公开页面、法律页面或可复现访客状态直接支持。
- `【合理推断】`：由至少两项兼容事实支持，但未看到后台实现。
- `【建议设计】`：为了可复刻性、安全、隐私、恢复或产品效果提出的方案。
- `【未知】`：未登录、未提交、不可访问、冲突或没有结果面证据。

## 关键证据边界

1. 页面上的“StarAsk”是产品/助手品牌；没有看到独立 Agent 名称或真实工具名。因此报告使用 `<问事分析与追踪职能>` 等功能命名，均非官方 Agent/工具名。
2. 官网示例展示预测时间线、关系对照与每日节奏；它们是页面演示，不等于本次访客运行结果。
3. 网站版为本次浏览自动建立访客账号 `#[REDACTED_GUEST_ID]`，交易记录显示“受邀注册奖励 +100”。这是本会话可见状态，不推断为所有用户固定规则。
4. 报告生成链需要出生资料、双手掌纹、正面照片与 50 题；本次在上传/提交前停止，无法验证识别、模型、报告质量、资产保存或删除。
5. 问事输入框、语音、风格、好友选择、追踪与历史均可见；本次未发送问题，因此回答生成、扣费、追踪自动创建、重试与分享均为 `【未知】`。
6. 页面截图接口在任意测试站点均返回无法成像；未伪造截图。以 18 份带 URL、标题、可见文本与 DOM 的页面快照替代，并在 `evidence/page-captures/` 留存。

## 完成判定

“页面说已完成”不足以证明任务完成。对问事/报告场景，完整完成至少需要同时检查：运行状态、结果资产、额度变化、上下文写入、可打开预览、用户确认与后续追踪/交付。本次没有触发生成，因此不宣称任何生成任务完成。

## 分析停止点

本项目止于可见产品行为、功能边界、语义架构与功能等价设计；不主张 StarAsk 的隐藏提示词、私有 API、模型供应商、数据库、队列、云厂商或内部推理。
